name: Staging Smoke Test

on:
  push:
    branches:
      - staging
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test
        run: echo "staging smoke ok"
