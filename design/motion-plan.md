# Microinteraction & Motion Plan

1. **Tool Cards Hover**
   - Scale + shadow already live; ergänzen Gradient border + subtle icon animation.
2. **Hero Live-Leads**
   - Auto-rotate Cases (fade) alle 6s (CSS animation + JS fallback).
3. **CTA Buttons**
   - Pulse outline on scroll-in (IntersectionObserver → add class).
4. **Form Inputs**
   - Floating labels + success checkmarks.
5. **Silo KPI Counters**
   - Count-up animation (requestAnimationFrame) bei Sichtbarkeit.

Todos warten auf Freigabe für JS/CSS-Animationsbudget.
